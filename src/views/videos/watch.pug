extends ../base


block content 
    div 
        video(src="/" + video.videoUrl,controls,width="1000",height="600") 
        p=video.description
        p=video.hashtags
        small=video.creatAt
        
   
    a(href=`${video.id}/edit`) Edit Video &rarr;
    br
    a(href=`${video.id}/delete`) Delete Video &rarr;