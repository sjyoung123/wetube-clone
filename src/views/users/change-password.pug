extends ../base

block content 
    if errorMessage
        span=errorMessage
    form(method="POST")
        input(type="password",placeholder="old password",name="oldPass")
        input(type="password",placeholder="new password",name="newPass")
        input(type="password",placeholder="new password confirmation",name="newPassConfirm")
        input(type="submit")